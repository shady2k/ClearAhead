## OrbitCamera — камера, вращающаяся вокруг точки на земле. Порт из снесённого
## спайка (`spike_relief.gd`, 809280f^), а не сочинение заново.
##
## # ОНА ТЕПЕРЬ ОДНА НА ВСЕ ТРИ РОЛИ
##
## Рядом жила вторая — FreeCamera, наблюдатель, ЛЕТАЮЩИЙ по миру: у неё было
## положение и взгляд, и оба менялись независимо. Ею смотрел машинист. Снесена
## 2026-08-12 (вечер) вместе с файлом, и довод владельца записан дословно:
## «машинист сейчас — это не персонаж, а просто летающая камера». Летающая камера
## на высоте глаза даёт похожий кадр и не проверяет ничего — у неё нет ни роста,
## ни тени, ни походки. Теперь у машиниста ЧЕЛОВЕК (driver.gd), а эта камера
## достаётся ему ОБЗОРНЫМ ВИДОМ — орбитой вокруг него самого.
##
## Что осталось от разницы двух камер: в видах от первого и третьего лица эту
## камеру ведёт голова человека, и на время этих видов у неё отбирают жесты
## (Driver.set_active). Смешанного органа управления по-прежнему нет — есть два,
## и переключаются они целиком, а не посреди жеста.
##
## # Что здесь ЗАКОННО константами
##
## Азимут, возвышение, ширина кадра, скорость орбиты и зума — свойства ВЗГЛЯДА.
## Граница ClearAhead-sjq разрешает клиенту камеру целиком: «вид не есть мир».
##
## И чего здесь НЕТ, хотя у спайка было: ТОЧКИ ФОКУСА КОНСТАНТОЙ. Снесённый
## клиент держал `shot_focus = Vector2(240.0, 0.0)` и `size = 300.0` — числа,
## взятые ниоткуда, и это одна из причин, по которым тот клиент снесли. Фокус
## приходит СНАРУЖИ: из габаритов того, что приехало с сервера, либо от человека,
## если роль пешая.
class_name OrbitCamera
extends Camera3D

## Поле зрения перспективного режима. Оно же переводит ширину орто-кадра в
## дистанцию, чтобы переключение проекции не прыгало по масштабу.
const CAM_FOV := 50.0

const ORBIT_RATE := 0.30      # градусов на пиксель
const ZOOM_STEP := 0.88       # во сколько раз меняется масштаб за щелчок колеса
## Основание для НЕПРЕРЫВНОГО зума жестом. У колеса есть щелчок и постоянный
## шаг, у двупальцевого скролла — только величина смещения, и шаг для него
## обязан считаться степенью. Число перенесено из спайка вместе с остальным
## лечением мака: 1.06 в степени типичного delta.y даёт на трекпаде примерно ту
## же скорость, что щелчок колеса на мыши.
const ZOOM_PAN_BASE := 1.06
const PAN_RATE := 0.55        # доля видимого поперечника в секунду
const PAN_FAST := 4.0         # множитель с Shift

const ELEV_MIN := 2.0
const ELEV_MAX := 89.0
const SIZE_MIN := 8.0
const DIST_MIN := 8.0
## ВЕРХНИЕ ПРЕДЕЛЫ БЕЗ МИРА. Здесь стояли SIZE_MAX = 8000 и DIST_MAX = 20000 как
## настоящий потолок зума, и это было ошибкой того же рода, что зашитая точка
## фокуса: камера, не знающая ни дальности взгляда, ни охвата региона, объявляла
## своим числом, сколько мира можно показать.
##
## Кадр владельца, которым это поймано: отъезд на восемь километров при мире,
## загруженном на два. Дымка выводится из дальности взгляда, и на восьми
## километрах она съедает 99.9 % — то есть камера смотрела дальше, чем сама же
## объявила видимость, и показывала белое пятно.
##
## Действующий потолок приходит СНАРУЖИ (WorldBounds), как приходят углы и точка
## фокуса. Эти два числа остались тем, чем и обязаны быть, — защитой от
## бесконечности там, где мира ещё нет: до загрузки и в проверках.
const SIZE_CEILING := 8000.0
const DIST_CEILING := 20000.0

## # УПОР В ЗЕМЛЮ
##
## Возвышение — угол от точки взгляда, а точка взгляда сидит НА ЗЕМЛЕ (человек,
## машина). Значит высота камеры над той же землёй равна `dist · sin(возв)`, и
## при угле у горизонта камера идёт вдоль самой поверхности. Стоит земле
## подниматься к ней круче, чем задран угол, — глаз оказывается ВНУТРИ грунта, а
## покров односторонний (`ground_look.gd`, `render_mode cull_back`): изнутри
## земли не рисуется вовсе. Наружу это выходит кадром, где нижняя половина экрана
## — нижняя полусфера неба цвета дымки, балласт висит лентой в пустоте, а трава
## растёт из ничего.
##
## ЗУМ ОТ ЭТОГО НЕ СПАСАЕТ, и это не мнение: и высота, и вынос в сторону
## пропорциональны `dist`, отношение при зуме не меняется. Против откоса призмы
## 1:1.5 (34°) тонет любой угол ниже 34° на любом масштабе.
##
## Замер, которым беда поймана (зонд камеры, ST_A, обзорный кадр 60 м → отступ
## 64.3 м): при 45° под камерой 54 м земли, при 2° — от 1.9 м у приводов стрелок.
## Полтора метра запаса на станции, стоящей на насыпи-плато; любой пригорок выше
## двух метров в радиусе 64 м — и камера внутри него.
##
## # Чем чиним и чего это стоит
##
## ТРИ СРЕДСТВА, И ПОРЯДОК ИХ НЕ ПЕРЕСТАВЛЯЕТСЯ — он же порядок цены.
##
## Первое — ШТАНГА: луч от точки взгляда к камере, и упёрся — подтянуть камеру
## ближе. Угол, который выкрутил игрок, сохраняется целиком; платит кадр
## (становится ближе). Так делает третье лицо всюду, у Godot это готовый узел
## SpringArm3D, и взят он не был по одной причине: камера здесь СЧИТАЕТ своё
## место из углов и масштаба, а узел-штанга требует, чтобы место считал он.
##
## Второе — ПОДЪЁМ УГЛА: угол принадлежит игроку, и отбирать его можно, только
## когда подтягивать уже некуда (штанга упёрлась в ARM_MIN_M).
##
## Третье — ПОЛ ПО ЗЕМЛЕ (_floor_up): страховка на то, чего не ловит луч вдоль
## штанги — гребень, пройденный по касательной. Замером она и заведена, разбор
## там же.
##
## ЧЕТВЁРТОЕ СРЕДСТВО, КОТОРОГО ЗДЕСЬ НЕТ: поднятая точка взгляда. Она есть, но
## живёт не тут, а у тех, кто фокус задаёт (Driver._orbit_focus,
## World._look_at_machine): подъём берётся из ГАБАРИТА — половина роста человека,
## половина паспортной высоты машины, — а габарит камере не принадлежит.
##
## ЧЕГО ЗДЕСЬ НЕТ: двустороннего покрова. Смотреть сквозь грунт на подкрашенное
## небо — не меньшая ложь, чем смотреть на голубое, и в мире это уже записано
## теми же словами («это не лечение, а смягчение», world.gd о нижней полусфере).
##
## ТОЛЬКО ПЕРСПЕКТИВА. В ортографии положение камеры на масштаб не влияет вовсе,
## а ближняя плоскость нарочно уведена ЗА камеру (`near = -_dist`), чтобы передний
## план не срезался. Подтянуть камеру там значит подтянуть и это отсечение — то
## есть срезать ровно тот передний план, ради которого приём и заведён. Остаток
## беды в ортографии назван и оставлен: она бывает у строителя, а тот смотрит с
## километров, где до земли не достать.

## Запас между глазом и поверхностью, метры. ПО НОРМАЛИ к ней, а не вдоль штанги
## (см. _pull_in): вдоль штанги тот же метр на пологом склоне даёт до земли
## сантиметры.
##
## Метр — это высота САМОЙ ВЫСОКОЙ ТРАВЫ, а не круглое число: у метёлки
## (Vegetation.GRASS_KINDS[2]) верх 0.62 м, разброс жребием до ×1.45, итого 0.90 м.
## Глаз, севший ниже, смотрит сквозь пучок изнутри.
const GROUND_CLEAR_M := 1.0
## Короче этого штангу не режем — дальше платит уже не кадр, а смысл кадра: при
## обзорном кадре в 60 м четыре метра до точки взгляда это не «вид на машину», а
## упор носом в её борт. Уперлись — забираем угол (см. _fit_to_solid).
const ARM_MIN_M := 4.0
## Насколько пологим считается касание, за которым отход по нормали перестаёт
## расти. Без этого предела грань, задетая по касательной, требует бесконечного
## отхода: cos угла между штангой и нормалью там уходит в ноль.
const ARM_GRAZE_MIN := 0.25
## За сколько секунд штанга возвращается на запрошенное место, когда преграда
## ушла. ЗАЖИМАЕТСЯ МГНОВЕННО, ОТПУСКАЕТСЯ ПЛАВНО, и несимметрично это нарочно:
## симметричное сглаживание даёт камере «дыхание» на каждой кочке, а запоздалый
## зажим — кадр, где земля успела закрыть экран.
const ARM_RELEASE_S := 0.30
## Ступеней двоичного поиска угла, при котором штанга свободна. Пять шагов на
## диапазоне до 87° дают точность 2.7° — меньше, чем шаг орбиты за одно движение
## мыши (ORBIT_RATE · 10 пикселей).
##
## Поиск законен потому, что свобода МОНОТОННА по углу: земля — поле высот, у неё
## над каждым азимутом один горизонт, и выше него луч свободен всегда.
const LIFT_STEPS := 5
## Слой тверди МИРА. Тела (машины, человек) лежат на втором слое и камерой не
## видны нарочно: камера, спотыкающаяся о свою же машину, подтягивалась бы к её
## борту всякий раз, а о чужую — прыгала бы при каждом встречном поезде.
const WORLD_SOLID_MASK := 1

## Упор изменился: доехали до края мира, отошли от него или уперлись в землю.
## Строкой, а не булевым: панель обязана назвать ПРИЧИНУ, иначе глухая стена
## неотличима от поломки. Пустая строка значит «упора нет».
signal limit_changed(note: String)

var focus := Vector3.ZERO      # точка интереса в осях Godot
var azimuth_deg := 205.0
var elev_deg := 45.0
var frame_size_m := 300.0      # видимая высота кадра в ортографии
var ortho := true

## Край мира. Пока его нет (камера построена, мир не загружен), камера ходит по
## потолкам выше — иначе её пришлось бы держать неподвижной до конца загрузки.
var bounds: WorldBounds = null

var _dist := 300.0
## На сколько упор УКОРОТИЛ штангу и НА СКОЛЬКО поднял угол — то, что отнято у
## запрошенного. Держится отдельно от _dist и elev_deg нарочно: запрошенное
## принадлежит игроку и упором не портится, иначе отъехавшая от склона камера не
## вернулась бы на своё место сама.
##
## Отнятое, а не готовая длина, и это цена одного кадра: с готовой длиной зум
## наружу пришлось бы догонять сглаживанием, и каждое движение колеса на чистом
## месте выглядело бы вязким.
var _cut_m := 0.0
var _lift_deg := 0.0
## Спрашивать ли твердь. Приходит СНАРУЖИ, как bounds: у камеры в чистых
## проверках мира нет вовсе, а до _build_solid его нет и в игре.
var _solid := false
var _limit_note := ""
var _world_note := ""
var _ground_note := ""
## Пока камера не в игре, ввод ей не принадлежит: в паузе и в меню жесты мыши
## обязаны доставаться кнопкам, а не станции под ними.
var _active := true
## Клавиатурная панорама. Мышь от этого флага не зависит — разбор у set_keys.
var _keys := true


## configure — навестись на то, что приехало.
##
## centre и size_m считает вызывающий из ГАБАРИТОВ ДАННЫХ. Углы и проекция —
## свойства взгляда и приходят от роли.
func configure(centre: Vector3, size_m: float, az_deg: float, el_deg: float, orthographic: bool) -> void:
	focus = centre
	frame_size_m = clampf(size_m, SIZE_MIN, SIZE_CEILING)
	azimuth_deg = az_deg
	elev_deg = clampf(el_deg, ELEV_MIN, ELEV_MAX)
	ortho = orthographic
	_dist = _dist_for(frame_size_m)
	# Упор снимается вместе с наводкой: configure зовут, когда камеру переносят на
	# другое место (сел в машину, вышел из неё), и притащить туда штангу, укороченную
	# о склон в прежнем месте, значило бы показать первый кадр не тем, чем он есть.
	_cut_m = 0.0
	_lift_deg = 0.0
	apply()


func set_active(on: bool) -> void:
	_active = on


## set_solid — объявить камере, что твердь мира построена и её можно спрашивать.
##
## Отдельным вызовом, а не проверкой «есть ли что-то в мире»: до _build_solid луч
## не встретил бы ничего и упор молча не работал бы, а молчащий упор неотличим от
## сломанного. Чистые проверки камеру в мир не ставят вовсе и этого не зовут.
func set_solid(on: bool) -> void:
	_solid = on


## set_keys — отдать или забрать у камеры КЛАВИАТУРНУЮ панораму, не трогая мышь.
##
## Отдельно от set_active, потому что это разные вопросы. set_active отвечает
## «смотрит ли игрок этой камерой»; здесь — «чьи сейчас буквы». В кабине камера
## по-прежнему смотрит и по-прежнему водится мышью, а WASD принадлежит машине:
## ноги у сидящего выключены, и клавиши свободны.
##
## Заведено 2026-08-15 по слову владельца: «когда мы в кабине, мы управляем
## камерой мышью, как это уже реализовано во многих играх». До того W в кабине
## панорамировал обзор, и снаружи это выглядело так, будто машинист не управляет
## ничем. Прежнее устройство держалось на том, что буквы заняты ходьбой, — а в
## кабине они не заняты ничем: ноги там выключены целиком. Раскладку рукояток с
## тех пор присылает сервер (content.Organ), и разбирает её world._cab_action.
func set_keys(on: bool) -> void:
	_keys = on


## set_bounds — объявить камере упоры и СРАЗУ их применить.
##
## Применить сразу — не мелочь, а пункт правила: предел по зуму выведен из
## дальности взгляда, и стоило игроку УМЕНЬШИТЬ дальность, как разрешённый кадр
## сузился под уже отъехавшей камерой. Оставить её снаружи значило бы показать
## после смены настройки ровно тот кадр, против которого предел и заведён.
func set_bounds(b: WorldBounds) -> void:
	bounds = b
	apply()


## limit_note — почему дальше нельзя. Пусто, пока никуда не упирались.
func limit_note() -> String:
	return _limit_note


func _dist_for(size_m: float) -> float:
	return size_m / (2.0 * tan(deg_to_rad(CAM_FOV) * 0.5))


## apply — собрать положение камеры из точки, углов и масштаба.
## standoff_m — на сколько метров камера отставлена от точки взгляда.
##
## Наружу это нужно ДЫМКЕ. В ортографии отступ на масштаб не влияет, но дымка
## считается от положения камеры, а не от того, куда смотрят: при кадре в 4 км
## камера стоит в 4.4 км, то есть вдвое дальше объявленной видимости, и весь кадр
## выбеливается. Спайк это знал и записал тем же числом: «отступ в 6 км даёт
## верную геометрию и полностью выбеленный дымкой кадр». Мир отодвигает начало
## глубинного тумана на этот отступ, и дымка начинает мерить удаление ОТ ПЛОСКОСТИ
## ФОКУСА, а не от камеры.
## ОТСТУП НАЗЫВАЕТСЯ ДЕЙСТВУЮЩИЙ, а не запрошенный: подтянутая к склону камера
## стоит ближе, и дымка, начатая на запрошенном отступе, оставила бы чистым весь
## кадр. Обе величины меряются от того, на что смотрят, — тем и держится правило.
func standoff_m() -> float:
	return _eff_dist()


## _eff_dist — сколько до точки взгляда НА САМОМ ДЕЛЕ.
func _eff_dist() -> float:
	if ortho or not _solid:
		return _dist
	return maxf(_dist - _cut_m, minf(ARM_MIN_M, _dist))


## _dir — единичный вектор от точки взгляда к камере по азимуту и возвышению.
func _dir(az_deg: float, el_deg: float) -> Vector3:
	var az := deg_to_rad(az_deg)
	var el := deg_to_rad(el_deg)
	return Vector3(cos(az), 0.0, sin(az)) * cos(el) + Vector3.UP * sin(el)


func apply() -> void:
	_clamp_to_world()
	if ortho:
		# В ортографии дистанция на масштаб не влияет вовсе, но держим её
		# согласованной с размером кадра: иначе переключение проекции прыгало бы.
		_dist = _dist_for(frame_size_m)
	position = focus + _dir(azimuth_deg, elev_deg + _lift_deg) * _eff_dist()
	look_at(focus, Vector3.UP)
	projection = PROJECTION_ORTHOGONAL if ortho else PROJECTION_PERSPECTIVE
	size = frame_size_m
	fov = CAM_FOV
	far = maxf(_dist * 8.0, 4000.0)
	# БЛИЖНЯЯ ПЛОСКОСТЬ В ОРТОГРАФИИ УХОДИТ ЗА КАМЕРУ, и это не хитрость, а
	# перенесённое лечение обрезанного края (разбор — в спайке, дословно).
	#
	# В ортографии дистанция на масштаб не влияет ВОВСЕ — влияет только
	# отсечение, и ближняя плоскость там параллельна экрану. На ширине кадра
	# 40 м камера стоит в сорока метрах, то есть ВНУТРИ сцены, и весь передний
	# план ближе неё срезается: на экране это прямая линия поперёк кадра.
	#
	# Отодвинуть камеру нельзя, хотя картинку это бы не изменило: от ПОЛОЖЕНИЯ
	# камеры считаются дымка (fog_density) и дальность зерна земли (detail_far в
	# шейдере покрова). Проверено снимком спайка: отступ в 6 км даёт верную
	# геометрию и полностью выбеленный дымкой кадр.
	#
	# У ортографической проекции ближняя плоскость — обычная граница линейного
	# отображения глубины, и отрицательной ей быть не запрещено: она просто
	# оказывается ПОЗАДИ камеры. Так сцена целиком попадает в объём отсечения, а
	# камера остаётся там, где её ждут дымка и шейдер. В перспективе так нельзя:
	# там ближняя плоскость стоит в знаменателе.
	near = -_dist if ortho else 0.05


## _clamp_to_world — ВЕСЬ КАДР ОСТАЁТСЯ ВНУТРИ ЗАГРУЖЕННОГО МИРА.
##
## Двух проверок хватает ровно потому, что они об одном: положение отвечает за
## «где стою», ширина кадра — за «докуда смотрю», и мир кончается там, где
## кончается засеянное (разбор целиком — в шапке WorldBounds).
##
## Одно место на все жесты: и колесо, и щипок, и клавиши, и панорама сходятся в
## apply(), поэтому упор нельзя обойти жестом, о котором забыли.
func _clamp_to_world() -> void:
	var note := ""
	if bounds != null and bounds.max_frame_m > 0.0:
		# В ортографии масштаб держит ширина кадра, в перспективе — дистанция
		# (см. _zoom_by). Поэтому и предел применяется к разным величинам, а не к
		# одной «на всякий случай».
		if frame_size_m > bounds.max_frame_m:
			frame_size_m = bounds.max_frame_m
			if ortho:
				note = "кадр упёрся в %.0f м: дальше загруженного мира нет — дальность взгляда %.0f м" % [
					bounds.max_frame_m, bounds.reach_m]
		var dist_max := _dist_for(bounds.max_frame_m)
		if _dist > dist_max:
			_dist = dist_max
			if not ortho:
				note = "отъезд упёрся в %.0f м кадра: дальше загруженного мира нет — дальность взгляда %.0f м" % [
					bounds.max_frame_m, bounds.reach_m]
	if bounds != null and bounds.outside(focus) > 0.0:
		focus = bounds.pull_in(focus)
		note = "камера упёрлась в край региона: дальше %.0f м от оси мира нет ни у клиента, ни у сервера" % [
			bounds.max_offset_m]
	_world_note = note
	_publish_note()


## _publish_note — упоров ДВА, а строка одна, и это не лень.
##
## Панель называет то, ВО ЧТО упёрлись сейчас; край мира и земля упираются в
## разное и одновременно бывают редко. Край мира назван первым: он про то, чего
## нет вовсе, а земля — про то, что временно мешает, и жаловаться на второе,
## доехав до первого, значило бы назвать меньшую беду.
func _publish_note() -> void:
	var note := _world_note if _world_note != "" else _ground_note
	if note == _limit_note:
		return
	_limit_note = note
	limit_changed.emit(note)


## _physics_process — упор в землю считается ШАГОМ ФИЗИКИ, а применяется в apply().
##
## Не в _process и не прямо в apply, и это не вкус: твердь спрашивается у
## PhysicsDirectSpaceState3D, а он законен только внутри физического шага. apply()
## же зовут откуда угодно — из жеста мыши, из хода машины, из наводки, — и
## запрос оттуда в лучшем случае отвечает состоянием прошлого шага, а в худшем
## ругается в лог. Здесь считается ЦЕЛЬ, apply() применяет уже посчитанное.
func _physics_process(delta: float) -> void:
	# НЕАКТИВНОЙ КАМЕРЕ СЮДА НЕЛЬЗЯ, и это не экономия. В видах от первого и
	# третьего лица тот же самый узел ведёт голова человека (Driver.place_camera),
	# и упор, продолжающий ставить камеру по орбите, отнимал бы её у него каждый
	# шаг физики: глаз дёргался бы между затылком и склоном.
	if not _active or not _solid or ortho:
		return
	var space := _space()
	if space == null:
		return
	var fit := _fit_to_solid(space)
	var want_cut: float = maxf(_dist - fit.x, 0.0)
	var want_lift: float = fit.y
	# ЗАЖИМ МГНОВЕННЫЙ, ОТПУСК ПЛАВНЫЙ — разбор у ARM_RELEASE_S.
	if want_cut > _cut_m:
		_cut_m = want_cut
	else:
		_cut_m = move_toward(_cut_m, want_cut, _dist / ARM_RELEASE_S * delta)
	if want_lift > _lift_deg:
		_lift_deg = want_lift
	else:
		_lift_deg = move_toward(_lift_deg, want_lift, ELEV_MAX / ARM_RELEASE_S * delta)
	if _lift_deg > 0.05:
		_ground_note = "камера поднялась над землёй: %.0f° вместо %.0f° — ниже подтягивать уже некуда" % [
			elev_deg + _lift_deg, elev_deg]
	elif _cut_m > 0.05:
		_ground_note = "камера подтянулась к точке взгляда: %.0f м вместо %.0f — между глазом и ней земля" % [
			_eff_dist(), _dist]
	else:
		_ground_note = ""
	_publish_note()
	apply()


## _space — твердь, если камера вообще стоит в мире. Пустая проверка не
## формальность: в чистых проверках камера живёт в дереве без мира.
func _space() -> PhysicsDirectSpaceState3D:
	if not is_inside_tree():
		return null
	var w := get_world_3d()
	return null if w == null else w.direct_space_state


## _fit_to_solid — какими штангой и добавкой к углу камера окажется НЕ В ЗЕМЛЕ.
## Отдаёт пару (метры, градусы); (запрошенное, 0) значит «ничто не мешает».
func _fit_to_solid(space: PhysicsDirectSpaceState3D) -> Vector2:
	var dir := _dir(azimuth_deg, elev_deg)
	var hit := _ray(space, focus, focus + dir * _dist)
	if hit.is_empty():
		return _floor_up(space, Vector2(_dist, 0.0))
	var arm := _pull_in(hit, dir)
	if arm >= ARM_MIN_M:
		return _floor_up(space, Vector2(arm, 0.0))
	# Подтягивать некуда — забираем угол. Ищем НАИМЕНЬШУЮ добавку, при которой
	# штанга свободна на всю запрошенную длину: наименьшую потому, что угол чужой,
	# и брать у игрока больше необходимого нельзя.
	var lo := 0.0
	var hi := ELEV_MAX - elev_deg
	if hi <= 0.0:
		return Vector2(maxf(arm, ARM_MIN_M), 0.0)
	for _i in LIFT_STEPS:
		var mid := (lo + hi) * 0.5
		if _ray(space, focus, focus + _dir(azimuth_deg, elev_deg + mid) * _dist).is_empty():
			hi = mid
		else:
			lo = mid
	var lifted := _dir(azimuth_deg, elev_deg + hi)
	var hit2 := _ray(space, focus, focus + lifted * _dist)
	if hit2.is_empty():
		return _floor_up(space, Vector2(_dist, hi))
	# Поднялись до потолка, а свободы нет: точка взгляда сама под землёй (человек
	# в выемке, машина в тоннеле, которого ещё нет). Тогда штанга берёт что может,
	# и это честнее, чем показать кадр изнутри грунта.
	return _floor_up(space, Vector2(maxf(_pull_in(hit2, lifted), ARM_MIN_M), hi))


## _floor_up — ПОЛ ПО ЗЕМЛЕ: последний рубеж, после штанги и подъёма.
##
## Зачем он, если штанга уже не пускает камеру в грунт: штанга держит запас ПО
## НОРМАЛИ К ЗАДЕТОЙ ГРАНИ, а над выпуклостью — над гребнем насыпи, над бровкой
## выемки — задетой грани нет вовсе. Луч проходит над гребнем по касательной, не
## встретив его, а земля за гребнем поднимается к камере уже без пересечения.
##
## ЭТО НЕ ДОГАДКА, А ЗАМЕР: до этого рубежа перебор зонда (3600 углов, ST_A) не
## находил ни одной камеры под землёй, но худший отвесный зазор был 0.12 м —
## то есть глаз сидел в траве, формально оставаясь над грунтом.
##
## Проход ОДИН, а не до сходимости. Подъём угла уводит камеру ближе к точке
## взгляда, там под ней уже другая земля, и строго тут нужен был бы цикл. Второй
## проход не взят потому, что он платит лучом за сотые доли метра: земля под
## камерой не отвес, и поправка второго круга меньше шага сетки рельефа.
func _floor_up(space: PhysicsDirectSpaceState3D, fit: Vector2) -> Vector2:
	var arm: float = fit.x
	var lift: float = fit.y
	var pos := focus + _dir(azimuth_deg, elev_deg + lift) * arm
	var down := _ray(space, pos, pos + Vector3.DOWN * GROUND_CLEAR_M)
	if down.is_empty():
		return fit
	var want_y := (down["position"] as Vector3).y + GROUND_CLEAR_M
	var sin_need := clampf((want_y - focus.y) / maxf(arm, 0.01), -1.0, 1.0)
	var need := rad_to_deg(asin(sin_need)) - elev_deg
	return Vector2(arm, clampf(maxf(lift, need), 0.0, ELEV_MAX - elev_deg))


## _pull_in — на какой длине штанги глаз отойдёт от задетой грани на
## GROUND_CLEAR_M ПО НОРМАЛИ.
##
## По нормали, а не вдоль штанги, и это главное здесь. Вдоль штанги запас
## обращается в свою проекцию: на откосе 34°, задетом взглядом у горизонта, метр
## по штанге даёт до поверхности 0.6 м — меньше высоты метёлки, то есть глаз
## сидит внутри травы, хотя формально «отошёл на метр».
func _pull_in(hit: Dictionary, dir: Vector3) -> float:
	var d := focus.distance_to(hit["position"] as Vector3)
	var slope: float = maxf(absf(dir.dot(hit["normal"] as Vector3)), ARM_GRAZE_MIN)
	return d - GROUND_CLEAR_M / slope


func _ray(space: PhysicsDirectSpaceState3D, from: Vector3, to: Vector3) -> Dictionary:
	var q := PhysicsRayQueryParameters3D.create(from, to)
	q.collision_mask = WORLD_SOLID_MASK
	var hit: Dictionary = space.intersect_ray(q)
	return hit
##
## Shift+ЛКМ как панорама — не украшение, а перенесённая грабля: у спайка
## панорама висела на средней и правой кнопке, а трекпад мака ни ту, ни другую
## перетаскиванием не изображает. Двупальцевый клик даёт ПКМ, но не даёт
## ПКМ+движение, и наружу это выходило так, что камера «привязана к одной
## точке»: орбита работает, а увести взгляд нечем.
##
## ЗУМ НА МАКЕ — вторая половина той же граблей, и она была ПОТЕРЯНА при переносе
## из спайка (лечение жило в spike_world.gd, 809280f^, а сюда переехал только
## родительский обработчик колеса). macOS отличает мышь от трекпада по фазе
## события прокрутки: у колеса фазы нет и приходит InputEventMouseButton с
## WHEEL_UP/WHEEL_DOWN, а у двупальцевого скролла фаза есть, и вместо колеса
## приходит InputEventPanGesture; щипок приходит InputEventMagnifyGesture.
## Колеса на трекпаде не бывает ВОВСЕ — поэтому камера, слушающая только его,
## снаружи выглядит так, будто зум сломан. Клавиши +/− — третье средство,
## работающее на любом вводе, включая тачпад без жестов и удалённый рабочий стол.
##
## Двупальцевый скролл отдан именно ЗУМУ, а не панораме, хотя на маке скролл
## обычно значит «прокрутить». Отвергнуто потому, что скролл здесь — тот же орган,
## что колесо мыши, и разное значение у одного органа на двух устройствах
## означало бы, что подсказка управления врёт одному из них.
func _unhandled_input(event: InputEvent) -> void:
	if not _active:
		return
	if event is InputEventMouseMotion:
		var mm := event as InputEventMouseMotion
		var mask := mm.button_mask
		if (mask & (MOUSE_BUTTON_MASK_LEFT | MOUSE_BUTTON_MASK_MIDDLE | MOUSE_BUTTON_MASK_RIGHT)) == 0:
			return
		var only_left := (mask & MOUSE_BUTTON_MASK_LEFT) != 0 \
			and (mask & (MOUSE_BUTTON_MASK_MIDDLE | MOUSE_BUTTON_MASK_RIGHT)) == 0
		# Shift берётся У САМОГО СОБЫТИЯ, а не опросом Input. Событие несёт
		# состояние модификаторов на момент, когда оно произошло, а
		# Input.is_key_pressed — на момент обработки, и это разные моменты:
		# движения мыши копятся за кадр (use_accumulated_input) и разбираются
		# позже. Отпущенный посреди протяжки Shift превращал бы хвост панорамы
		# в орбиту, и выглядело бы это как «камера иногда прыгает».
		if only_left and not mm.shift_pressed:
			_orbit(mm.relative)
		else:
			_pan(mm.relative)
		apply()
		get_viewport().set_input_as_handled()
	elif event is InputEventMouseButton and (event as InputEventMouseButton).pressed:
		var mb := event as InputEventMouseButton
		if mb.button_index == MOUSE_BUTTON_WHEEL_UP:
			_zoom(-1.0)
		elif mb.button_index == MOUSE_BUTTON_WHEEL_DOWN:
			_zoom(1.0)
		else:
			return
		apply()
		get_viewport().set_input_as_handled()
	elif event is InputEventPanGesture:
		_zoom_by(pow(ZOOM_PAN_BASE, (event as InputEventPanGesture).delta.y))
		apply()
		get_viewport().set_input_as_handled()
	elif event is InputEventMagnifyGesture:
		# factor у щипка — во сколько раз развели пальцы, то есть насколько
		# ПРИБЛИЗИТЬ. Наш множитель считает обратное (больше единицы — отдалить),
		# отсюда деление. Нижняя граница — защита от деления на ноль: движок
		# отдаёт factor как 1+magnification, и предела снизу у него нет.
		_zoom_by(1.0 / maxf((event as InputEventMagnifyGesture).factor, 0.01))
		apply()
		get_viewport().set_input_as_handled()
	elif event is InputEventKey and (event as InputEventKey).pressed and not (event as InputEventKey).echo:
		match (event as InputEventKey).keycode:
			KEY_P:
				toggle_projection()   # apply() зовёт сама: она меняет и проекцию
			KEY_EQUAL, KEY_KP_ADD:
				_zoom(-1.0)
				apply()
			KEY_MINUS, KEY_KP_SUBTRACT:
				_zoom(1.0)
				apply()
			_:
				return
		get_viewport().set_input_as_handled()


## Панорама клавишами. Шаг пропорционален тому, что видно в кадре, а не задан в
## метрах: с постоянным шагом вблизи камера улетает за кадр, а на общем плане
## ползёт.
func _process(delta: float) -> void:
	if not _active or not _keys:
		return
	var keys := Vector2.ZERO
	if Input.is_key_pressed(KEY_W) or Input.is_key_pressed(KEY_UP):
		keys.y += 1.0
	if Input.is_key_pressed(KEY_S) or Input.is_key_pressed(KEY_DOWN):
		keys.y -= 1.0
	if Input.is_key_pressed(KEY_D) or Input.is_key_pressed(KEY_RIGHT):
		keys.x += 1.0
	if Input.is_key_pressed(KEY_A) or Input.is_key_pressed(KEY_LEFT):
		keys.x -= 1.0
	if keys == Vector2.ZERO:
		return
	var span := frame_size_m if ortho else _dist
	var step := PAN_RATE * span * delta
	if Input.is_key_pressed(KEY_SHIFT):
		step *= PAN_FAST
	var m := keys.normalized() * step
	focus += _right() * m.x + _forward() * m.y
	apply()


## Плоские орты камеры: панорама идёт ПО ЗЕМЛЕ, а не по экрану. Иначе при
## наклоне 45° клавиша «вперёд» уводила бы фокус под землю.
func _right() -> Vector3:
	var b := global_transform.basis
	return Vector3(b.x.x, 0.0, b.x.z).normalized()


func _forward() -> Vector3:
	var b := global_transform.basis
	return Vector3(-b.z.x, 0.0, -b.z.z).normalized()


func _orbit(rel: Vector2) -> void:
	azimuth_deg -= rel.x * ORBIT_RATE
	elev_deg = clampf(elev_deg + rel.y * ORBIT_RATE, ELEV_MIN, ELEV_MAX)


func _pan(rel: Vector2) -> void:
	var vp := get_viewport().get_visible_rect().size
	if vp.y <= 0.0:
		return
	var wpp: float
	if ortho:
		wpp = frame_size_m / vp.y
	else:
		wpp = 2.0 * _dist * tan(deg_to_rad(CAM_FOV) * 0.5) / vp.y
	focus -= _right() * rel.x * wpp
	focus += _forward() * rel.y * wpp


## Зум щелчком: dir < 0 — приблизить, dir > 0 — отдалить. Шаг постоянный, потому
## что у колеса и у клавиши +/− нет величины — только факт.
func _zoom(dir: float) -> void:
	_zoom_by(ZOOM_STEP if dir < 0.0 else 1.0 / ZOOM_STEP)


## _zoom_by — множитель масштаба; больше единицы значит отдалиться.
##
## Отдельно от _zoom(dir) потому, что жест трекпада НЕПРЕРЫВЕН: у скролла и щипка
## есть величина, и загонять её в два фиксированных шага значило бы выбросить
## ровно то, чем жест отличается от колеса.
##
## Что именно меняется, зависит от проекции, и это не мелочь: в ортографии
## дистанция на масштаб не влияет вовсе (см. apply), поэтому там двигать надо
## ширину кадра, а в перспективе — дистанцию.
func _zoom_by(f: float) -> void:
	if ortho:
		frame_size_m = clampf(frame_size_m * f, SIZE_MIN, SIZE_CEILING)
	else:
		_dist = clampf(_dist * f, DIST_MIN, DIST_CEILING)


## toggle_projection — P. Масштаб сохраняется: в кадре остаётся то же, что было.
func toggle_projection() -> void:
	ortho = not ortho
	if ortho:
		frame_size_m = clampf(2.0 * _dist * tan(deg_to_rad(CAM_FOV) * 0.5), SIZE_MIN, SIZE_CEILING)
	else:
		_dist = clampf(_dist_for(frame_size_m), DIST_MIN, DIST_CEILING)
	apply()


func projection_name() -> String:
	return "ортографическая" if ortho else "перспективная"
